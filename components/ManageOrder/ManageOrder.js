import React, {useMemo} from 'react';
import {Text, View} from 'react-native';
const ManageOrder = activeItem => {
  
  return (
    <View>
      <Text>JSON.stringify(menu)</Text>
    </View>
  );
};

export default ManageOrder;
